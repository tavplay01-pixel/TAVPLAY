/**
 * Simple re-export of the audio context hook.
 * All audio logic is centralized in lib/audio-context.tsx using Web Audio API.
 */
export { useAudioContext as useAudio } from "@/lib/audio-context";
